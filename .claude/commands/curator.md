---
description: Run a curator audit of your Claude Code skills and MCP servers, then walk through the proposed cleanup actions.
---

Invoke the `curator` skill. Run a scan, read the latest report, and walk the user through clusters, duplicate MCPs, and orphans. For each finding, ask whether to check the action box. When the user is done, apply the checked actions.
